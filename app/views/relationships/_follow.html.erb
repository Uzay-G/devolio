<%= form_for(current_user.active_relationships.build, remote: true) do |f| %>
    <div><%= hidden_field_tag :followable_id, object.id %></div>
    <div><%= hidden_field_tag :followable_type, object.class.name %></div>
    <%= f.submit "Follow", class: "btn btn-primary" %>
  <% end %>